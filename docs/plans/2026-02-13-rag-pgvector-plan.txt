# RAG with pgvector Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Hub-AIのデータ提供をpgvectorベースRAG検索に移行し、トークン消費を80-90%削減する。

**Architecture:** Supabase pgvectorで統合embeddingテーブルを作成。データ変更時にEdge Function経由でOpenAI text-embedding-3-largeでembeddingを生成。AI Chat時はクエリをembedding化し、cosine similarity検索で関連データのみをコンテキストに含める。

**Tech Stack:** Supabase (pgvector, Edge Functions, DB Webhooks), OpenAI Embedding API (text-embedding-3-large), Deno (Edge Functions runtime)

---

## Task 1: pgvector拡張とdocument_embeddingsテーブル作成

**Files:**
- Create: `supabase/migrations/20260221120000_create_document_embeddings.sql`

**Step 1: マイグレーションファイルを作成**

```sql
-- Enable pgvector extension
CREATE EXTENSION IF NOT EXISTS vector;

-- Unified embedding storage table
CREATE TABLE document_embeddings (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  source_type TEXT NOT NULL,
  source_id TEXT NOT NULL,
  content_text TEXT NOT NULL,
  embedding vector(3072) NOT NULL,
  metadata JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(user_id, source_type, source_id)
);

-- Indexes
CREATE INDEX idx_embeddings_user ON document_embeddings(user_id, source_type);
CREATE INDEX idx_embeddings_vector ON document_embeddings
  USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100);

-- RLS
ALTER TABLE document_embeddings ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can read own embeddings"
  ON document_embeddings FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Service role can manage all embeddings"
  ON document_embeddings FOR ALL USING (true)
  WITH CHECK (true);

-- Similarity search function
CREATE OR REPLACE FUNCTION match_documents(
  query_embedding vector(3072),
  match_user_id UUID,
  match_count INT DEFAULT 10,
  match_threshold FLOAT DEFAULT 0.3
)
RETURNS TABLE (
  id UUID,
  source_type TEXT,
  source_id TEXT,
  content_text TEXT,
  metadata JSONB,
  similarity FLOAT
)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  RETURN QUERY
  SELECT
    de.id,
    de.source_type,
    de.source_id,
    de.content_text,
    de.metadata,
    1 - (de.embedding <=> query_embedding) AS similarity
  FROM document_embeddings de
  WHERE de.user_id = match_user_id
    AND 1 - (de.embedding <=> query_embedding) > match_threshold
  ORDER BY de.embedding <=> query_embedding
  LIMIT match_count;
END;
$$;
```

**Step 2: マイグレーションを適用**

Run: `pnpm exec supabase db push`
Expected: Migration applied successfully

**Step 3: テーブル作成を確認**

Supabase MCP の execute_sql ツールで確認:
```sql
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name = 'document_embeddings' ORDER BY ordinal_position;
```
Expected: id, user_id, source_type, source_id, content_text, embedding, metadata, created_at, updated_at

**Step 4: コミット**

```bash
git add supabase/migrations/20260221120000_create_document_embeddings.sql
git commit -m "feat: add pgvector extension and document_embeddings table for RAG"
```

---

## Task 2: generate_embedding Edge Function作成

**Files:**
- Create: `supabase/functions/generate_embedding/index.ts`

**Step 1: Edge Functionを作成**

```typescript
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
const serviceRoleKey =
  Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ??
  Deno.env.get("SERVICE_ROLE_KEY") ??
  "";
const openaiApiKey = Deno.env.get("OPENAI_API_KEY") ?? "";

const EMBEDDING_MODEL = "text-embedding-3-large";

type EmbeddingRequest = {
  source_type: string;
  source_id: string;
  content: string;
  metadata?: Record<string, unknown>;
  user_id: string;
};

type BulkEmbeddingRequest = {
  items: EmbeddingRequest[];
};

const generateEmbedding = async (text: string): Promise<number[]> => {
  const response = await fetch("https://api.openai.com/v1/embeddings", {
    method: "POST",
    headers: {
      Authorization: `Bearer ${openaiApiKey}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      model: EMBEDDING_MODEL,
      input: text.slice(0, 8000),
    }),
  });
  if (!response.ok) {
    const errText = await response.text();
    throw new Error(`OpenAI Embedding API error: ${response.status} ${errText}`);
  }
  const data = await response.json();
  return data.data[0].embedding;
};

Deno.serve(async (req) => {
  const corsHeaders = {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Access-Control-Allow-Headers":
      "authorization, x-client-info, apikey, content-type",
  };

  if (req.method === "OPTIONS") {
    return new Response(null, { status: 204, headers: corsHeaders });
  }

  if (!openaiApiKey) {
    return new Response(
      JSON.stringify({ error: "Missing OPENAI_API_KEY" }),
      { status: 500, headers: { ...corsHeaders, "Content-Type": "application/json" } },
    );
  }

  const supabaseAdmin = createClient(supabaseUrl, serviceRoleKey, {
    auth: { persistSession: false },
  });

  try {
    const body = await req.json();

    // Support both single and bulk requests
    const items: EmbeddingRequest[] = body.items
      ? (body as BulkEmbeddingRequest).items
      : [body as EmbeddingRequest];

    const results = [];
    for (const item of items) {
      if (!item.content || !item.source_type || !item.source_id || !item.user_id) {
        results.push({ source_id: item.source_id, error: "Missing required fields" });
        continue;
      }

      const embedding = await generateEmbedding(item.content);

      const { error: upsertError } = await supabaseAdmin
        .from("document_embeddings")
        .upsert(
          {
            user_id: item.user_id,
            source_type: item.source_type,
            source_id: item.source_id,
            content_text: item.content.slice(0, 10000),
            embedding: embedding as unknown as string,
            metadata: item.metadata ?? {},
            updated_at: new Date().toISOString(),
          },
          { onConflict: "user_id,source_type,source_id" },
        );

      if (upsertError) {
        results.push({ source_id: item.source_id, error: upsertError.message });
      } else {
        results.push({ source_id: item.source_id, status: "ok" });
      }
    }

    return new Response(
      JSON.stringify({ results }),
      { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } },
    );
  } catch (err) {
    const message = err instanceof Error ? err.message : "Unknown error";
    console.error("generate_embedding error:", message);
    return new Response(
      JSON.stringify({ error: message }),
      { status: 500, headers: { ...corsHeaders, "Content-Type": "application/json" } },
    );
  }
});
```

**Step 2: デプロイ**

Run: `pnpm exec supabase functions deploy generate_embedding --no-verify-jwt`
Expected: Function deployed successfully

**Step 3: 動作確認（手動テスト）**

Supabase MCP execute_sql で確認:
```sql
SELECT count(*) FROM document_embeddings;
```

**Step 4: コミット**

```bash
git add supabase/functions/generate_embedding/index.ts
git commit -m "feat: add generate_embedding edge function for RAG pipeline"
```

---

## Task 3: backfill_embeddings Edge Function作成（既存データ一括Embedding）

**Files:**
- Create: `supabase/functions/backfill_embeddings/index.ts`

**Step 1: バックフィル用Edge Functionを作成**

```typescript
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
const serviceRoleKey =
  Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ??
  Deno.env.get("SERVICE_ROLE_KEY") ??
  "";
const openaiApiKey = Deno.env.get("OPENAI_API_KEY") ?? "";

const EMBEDDING_MODEL = "text-embedding-3-large";

const generateEmbedding = async (text: string): Promise<number[]> => {
  const response = await fetch("https://api.openai.com/v1/embeddings", {
    method: "POST",
    headers: {
      Authorization: `Bearer ${openaiApiKey}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      model: EMBEDDING_MODEL,
      input: text.slice(0, 8000),
    }),
  });
  if (!response.ok) {
    throw new Error(`OpenAI error: ${response.status}`);
  }
  const data = await response.json();
  return data.data[0].embedding;
};

type SourceConfig = {
  table: string;
  source_type: string;
  buildContent: (row: Record<string, unknown>) => string;
  buildMetadata: (row: Record<string, unknown>) => Record<string, unknown>;
  idField?: string;
};

const SOURCE_CONFIGS: SourceConfig[] = [
  {
    table: "memos",
    source_type: "memo",
    buildContent: (r) => `${r.title ?? ""}: ${r.content ?? ""}`,
    buildMetadata: (r) => ({ title: r.title, tab_id: r.tab_id }),
  },
  {
    table: "journal_entries",
    source_type: "journal",
    buildContent: (r) => `${r.entry_date ?? ""} ${r.title ?? ""}: ${r.content ?? ""}`,
    buildMetadata: (r) => ({ title: r.title, entry_date: r.entry_date, mood: r.mood }),
  },
  {
    table: "google_gmail_messages",
    source_type: "gmail",
    idField: "message_id",
    buildContent: (r) => `From:${r.sender ?? ""} Subject:${r.subject ?? ""} ${r.body_text ?? r.snippet ?? ""}`,
    buildMetadata: (r) => ({ subject: r.subject, sender: r.sender, date: r.date }),
  },
  {
    table: "tasks",
    source_type: "task",
    buildContent: (r) => `${r.title ?? ""}${r.due_date ? ` (due:${r.due_date})` : ""}: ${r.notes ?? ""}`,
    buildMetadata: (r) => ({ title: r.title, status: r.status, due_date: r.due_date }),
  },
  {
    table: "media_feed_items",
    source_type: "media_feed",
    buildContent: (r) => `[${r.source ?? ""}] ${r.title ?? ""}: ${r.body ?? ""}`,
    buildMetadata: (r) => ({ title: r.title, source: r.source }),
  },
  {
    table: "projects",
    source_type: "project",
    buildContent: (r) => `${r.name ?? ""}: ${r.description ?? ""}`,
    buildMetadata: (r) => ({ name: r.name, status: r.status }),
  },
  {
    table: "clients",
    source_type: "client",
    buildContent: (r) => `${r.name ?? ""}: ${r.notes ?? ""}`,
    buildMetadata: (r) => ({ name: r.name, status: r.status }),
  },
];

Deno.serve(async (req) => {
  const corsHeaders = {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Access-Control-Allow-Headers":
      "authorization, x-client-info, apikey, content-type",
  };

  if (req.method === "OPTIONS") {
    return new Response(null, { status: 204, headers: corsHeaders });
  }

  const supabaseAdmin = createClient(supabaseUrl, serviceRoleKey, {
    auth: { persistSession: false },
  });

  // Authenticate
  const authHeader = req.headers.get("Authorization");
  if (!authHeader?.startsWith("Bearer ")) {
    return new Response(JSON.stringify({ error: "Unauthorized" }), {
      status: 401,
      headers: { ...corsHeaders, "Content-Type": "application/json" },
    });
  }
  const token = authHeader.slice(7);
  const { data: { user }, error: authError } = await supabaseAdmin.auth.getUser(token);
  if (authError || !user) {
    return new Response(JSON.stringify({ error: "Unauthorized" }), {
      status: 401,
      headers: { ...corsHeaders, "Content-Type": "application/json" },
    });
  }

  const userId = user.id;
  const summary: Record<string, { processed: number; errors: number }> = {};

  for (const config of SOURCE_CONFIGS) {
    const idField = config.idField ?? "id";
    summary[config.source_type] = { processed: 0, errors: 0 };

    // Fetch all rows for this user
    const { data: rows, error: fetchError } = await supabaseAdmin
      .from(config.table)
      .select("*")
      .eq("user_id", userId);

    if (fetchError || !rows) {
      console.error(`Failed to fetch ${config.table}:`, fetchError);
      continue;
    }

    for (const row of rows) {
      const sourceId = String(row[idField] ?? row.id);
      const content = config.buildContent(row);
      if (!content.trim() || content.trim() === ":") continue;

      try {
        // Check if already embedded
        const { data: existing } = await supabaseAdmin
          .from("document_embeddings")
          .select("id")
          .eq("user_id", userId)
          .eq("source_type", config.source_type)
          .eq("source_id", sourceId)
          .maybeSingle();

        if (existing) {
          summary[config.source_type].processed++;
          continue; // Skip already embedded
        }

        const embedding = await generateEmbedding(content);

        await supabaseAdmin.from("document_embeddings").upsert(
          {
            user_id: userId,
            source_type: config.source_type,
            source_id: sourceId,
            content_text: content.slice(0, 10000),
            embedding: embedding as unknown as string,
            metadata: config.buildMetadata(row),
            updated_at: new Date().toISOString(),
          },
          { onConflict: "user_id,source_type,source_id" },
        );

        summary[config.source_type].processed++;
      } catch (err) {
        console.error(`Embedding error for ${config.source_type}/${sourceId}:`, err);
        summary[config.source_type].errors++;
      }
    }
  }

  return new Response(
    JSON.stringify({ status: "completed", summary }),
    { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } },
  );
});
```

**Step 2: デプロイ**

Run: `pnpm exec supabase functions deploy backfill_embeddings --no-verify-jwt`

**Step 3: コミット**

```bash
git add supabase/functions/backfill_embeddings/index.ts
git commit -m "feat: add backfill_embeddings edge function for initial data indexing"
```

---

## Task 4: ai_hub_chat Edge Function改修（RAG検索統合）

**Files:**
- Modify: `supabase/functions/ai_hub_chat/index.ts`

**Step 1: OpenAI embedding関数とRAG検索をai_hub_chatに追加**

ai_hub_chat/index.ts の変更点:
1. OPENAI_API_KEY は既に読み込み済み (line 41)
2. `generateQueryEmbedding()` 関数を追加
3. `searchRelevantDocuments()` 関数を追加
4. メインハンドラで `dataContext` の代わりにRAG検索結果を使用

変更箇所:

a) line 43 の後に embedding 関数を追加:
```typescript
const EMBEDDING_MODEL = "text-embedding-3-large";

const generateQueryEmbedding = async (text: string): Promise<number[]> => {
  const response = await fetch("https://api.openai.com/v1/embeddings", {
    method: "POST",
    headers: {
      Authorization: `Bearer ${openaiApiKey}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      model: EMBEDDING_MODEL,
      input: text.slice(0, 8000),
    }),
  });
  if (!response.ok) {
    throw new Error(`Embedding API error: ${response.status}`);
  }
  const data = await response.json();
  return data.data[0].embedding;
};

const searchRelevantDocuments = async (
  supabaseAdmin: ReturnType<typeof createClient>,
  userId: string,
  queryText: string,
  matchCount = 10,
): Promise<string> => {
  const queryEmbedding = await generateQueryEmbedding(queryText);

  const { data: matches, error } = await supabaseAdmin.rpc("match_documents", {
    query_embedding: queryEmbedding,
    match_user_id: userId,
    match_count: matchCount,
    match_threshold: 0.3,
  });

  if (error || !matches || matches.length === 0) {
    return "";
  }

  const sections = matches.map(
    (m: { source_type: string; content_text: string; similarity: number; metadata: Record<string, unknown> }) => {
      const meta = m.metadata ?? {};
      const title = meta.title || meta.name || meta.subject || "";
      return `[${m.source_type}${title ? `: ${title}` : ""}] (relevance: ${(m.similarity * 100).toFixed(0)}%)\n${m.content_text}`;
    },
  );

  return `# Relevant Data (RAG Search Results)\n\n${sections.join("\n\n---\n\n")}`;
};
```

b) メインハンドラの dataContext 構築部分 (line 370-375) を変更:

変更前:
```typescript
  const dataContext = payload.dataContext ?? "";
  const systemInstruction = payload.systemInstruction ?? "";
  const userPrompt = buildUserPrompt(dataContext, payload.content);
```

変更後:
```typescript
  // Use RAG search if no dataContext provided from frontend
  let dataContext = payload.dataContext ?? "";
  if (!dataContext) {
    try {
      dataContext = await searchRelevantDocuments(
        supabaseAdmin,
        userId,
        payload.content,
        15,
      );
    } catch (ragErr) {
      console.error("RAG search failed, continuing without context:", ragErr);
    }
  }
  const systemInstruction = payload.systemInstruction ?? "";
  const userPrompt = buildUserPrompt(dataContext, payload.content);
```

**Step 2: デプロイ**

Run: `pnpm exec supabase functions deploy ai_hub_chat --no-verify-jwt`

**Step 3: コミット**

```bash
git add supabase/functions/ai_hub_chat/index.ts
git commit -m "feat: integrate RAG search into ai_hub_chat edge function"
```

---

## Task 5: フロントエンド useAiChat.ts 改修

**Files:**
- Modify: `src/hooks/useAiChat.ts`

**Step 1: SYSTEM_INSTRUCTION を更新 (line 135-161)**

変更前のSYSTEM_INSTRUCTIONを以下に置き換え:
```typescript
const SYSTEM_INSTRUCTION = `You are an AI assistant for an organization management app. Analyze the user's data and answer their questions.

You will receive relevant data retrieved via semantic search (RAG). The data includes:
- Memos, Journal entries, Gmail messages
- Tasks, Projects, Clients
- Media feed items (Slack, RSS, X/Twitter)
- And other organizational data

Each result shows its source type and relevance score.
If the retrieved data doesn't contain enough information to answer the question, say so honestly.

When referencing data, provide accurate information.
Answer in a concise and clear manner.
Use Markdown format for responses.`;
```

**Step 2: sendMessage() を改修 (line 808-832)**

変更前:
```typescript
        // Fetch current data
        const data = await fetchAllData();
        const dataContext = formatDataContext(data);
```

変更後:
```typescript
        // RAG search is handled server-side in the edge function
        // No need to fetch and format all data on the frontend
```

Edge Function呼び出しからdataContextを削除:
```typescript
        const { data: edgeFnData, error: edgeFnError } =
          await supabase.functions.invoke("ai_hub_chat", {
            body: {
              sessionId,
              sessionTitle,
              userMessageId: userMessage.id,
              content,
              provider,
              model,
              systemInstruction: SYSTEM_INSTRUCTION,
              ...(attachments.length > 0 ? { attachments } : {}),
            },
          });
```

**Step 3: 未使用のfetchAllData/formatDataContext依存を整理**

sendMessage の useCallback 依存配列 (line 884) を更新:
```typescript
    [/* fetchAllData, formatDataContext を削除 */],
```

注意: fetchAllData と formatDataContext 自体は削除しない。他の用途で使われる可能性があるため残す。将来的に不要と確認できたら別タスクで削除。

**Step 4: 動作確認**

Run: `pnpm exec tsc --noEmit`
Expected: No type errors

**Step 5: コミット**

```bash
git add src/hooks/useAiChat.ts
git commit -m "feat: switch useAiChat to server-side RAG, remove client-side data context"
```

---

## Task 6: DB Webhookによるリアルタイムembedding生成設定

**Files:**
- Create: `supabase/migrations/20260221120001_create_embedding_trigger_function.sql`

**Step 1: DB通知用関数のマイグレーションを作成**

```sql
-- Function to call generate_embedding edge function via pg_net
CREATE OR REPLACE FUNCTION notify_embedding_update()
RETURNS trigger
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
DECLARE
  source_type_val TEXT;
  source_id_val TEXT;
  content_val TEXT;
  metadata_val JSONB;
  edge_function_url TEXT;
  service_role_key TEXT;
BEGIN
  -- Determine source type from table name
  source_type_val := TG_ARGV[0];

  -- Build content based on source type
  CASE source_type_val
    WHEN 'memo' THEN
      source_id_val := NEW.id::TEXT;
      content_val := COALESCE(NEW.title, '') || ': ' || COALESCE(NEW.content, '');
      metadata_val := jsonb_build_object('title', NEW.title, 'tab_id', NEW.tab_id);
    WHEN 'journal' THEN
      source_id_val := NEW.id::TEXT;
      content_val := COALESCE(NEW.entry_date::TEXT, '') || ' ' || COALESCE(NEW.title, '') || ': ' || COALESCE(NEW.content, '');
      metadata_val := jsonb_build_object('title', NEW.title, 'entry_date', NEW.entry_date, 'mood', NEW.mood);
    WHEN 'gmail' THEN
      source_id_val := NEW.message_id;
      content_val := 'From:' || COALESCE(NEW.sender, '') || ' Subject:' || COALESCE(NEW.subject, '') || ' ' || COALESCE(NEW.body_text, COALESCE(NEW.snippet, ''));
      metadata_val := jsonb_build_object('subject', NEW.subject, 'sender', NEW.sender, 'date', NEW.date);
    WHEN 'task' THEN
      source_id_val := NEW.id::TEXT;
      content_val := COALESCE(NEW.title, '') || CASE WHEN NEW.due_date IS NOT NULL THEN ' (due:' || NEW.due_date::TEXT || ')' ELSE '' END || ': ' || COALESCE(NEW.notes, '');
      metadata_val := jsonb_build_object('title', NEW.title, 'status', NEW.status, 'due_date', NEW.due_date);
    WHEN 'media_feed' THEN
      source_id_val := NEW.id::TEXT;
      content_val := '[' || COALESCE(NEW.source, '') || '] ' || COALESCE(NEW.title, '') || ': ' || COALESCE(NEW.body, '');
      metadata_val := jsonb_build_object('title', NEW.title, 'source', NEW.source);
    WHEN 'project' THEN
      source_id_val := NEW.id::TEXT;
      content_val := COALESCE(NEW.name, '') || ': ' || COALESCE(NEW.description, '');
      metadata_val := jsonb_build_object('name', NEW.name, 'status', NEW.status);
    WHEN 'client' THEN
      source_id_val := NEW.id::TEXT;
      content_val := COALESCE(NEW.name, '') || ': ' || COALESCE(NEW.notes, '');
      metadata_val := jsonb_build_object('name', NEW.name, 'status', NEW.status);
    ELSE
      RETURN NEW;
  END CASE;

  -- Skip if content is empty
  IF TRIM(content_val) = '' OR TRIM(content_val) = ':' THEN
    RETURN NEW;
  END IF;

  -- Get edge function URL from vault or use default
  edge_function_url := current_setting('app.settings.supabase_url', true) ||
    '/functions/v1/generate_embedding';
  service_role_key := current_setting('app.settings.service_role_key', true);

  -- Call edge function via pg_net (async HTTP)
  PERFORM net.http_post(
    url := edge_function_url,
    headers := jsonb_build_object(
      'Content-Type', 'application/json',
      'Authorization', 'Bearer ' || service_role_key
    ),
    body := jsonb_build_object(
      'source_type', source_type_val,
      'source_id', source_id_val,
      'content', content_val,
      'metadata', metadata_val,
      'user_id', NEW.user_id
    )
  );

  RETURN NEW;
END;
$$;

-- Create triggers for each source table
CREATE TRIGGER trg_memo_embedding
  AFTER INSERT OR UPDATE OF title, content ON memos
  FOR EACH ROW EXECUTE FUNCTION notify_embedding_update('memo');

CREATE TRIGGER trg_journal_embedding
  AFTER INSERT OR UPDATE OF title, content ON journal_entries
  FOR EACH ROW EXECUTE FUNCTION notify_embedding_update('journal');

CREATE TRIGGER trg_gmail_embedding
  AFTER INSERT OR UPDATE OF subject, body_text ON google_gmail_messages
  FOR EACH ROW EXECUTE FUNCTION notify_embedding_update('gmail');

CREATE TRIGGER trg_task_embedding
  AFTER INSERT OR UPDATE OF title, notes ON tasks
  FOR EACH ROW EXECUTE FUNCTION notify_embedding_update('task');

CREATE TRIGGER trg_media_feed_embedding
  AFTER INSERT OR UPDATE OF title, body ON media_feed_items
  FOR EACH ROW EXECUTE FUNCTION notify_embedding_update('media_feed');

CREATE TRIGGER trg_project_embedding
  AFTER INSERT OR UPDATE OF name, description ON projects
  FOR EACH ROW EXECUTE FUNCTION notify_embedding_update('project');

CREATE TRIGGER trg_client_embedding
  AFTER INSERT OR UPDATE OF name, notes ON clients
  FOR EACH ROW EXECUTE FUNCTION notify_embedding_update('client');
```

注意: pg_net拡張が必要。Supabaseではデフォルトで有効。
また `app.settings.supabase_url` と `app.settings.service_role_key` はSupabase Database Settings > Custom Config で設定する必要がある。

代替案: pg_netが使えない場合は、Supabase Dashboard > Database > Webhooks からUIで設定する。

**Step 2: マイグレーション適用**

Run: `pnpm exec supabase db push`

**Step 3: コミット**

```bash
git add supabase/migrations/20260221120001_create_embedding_trigger_function.sql
git commit -m "feat: add DB triggers for real-time embedding generation"
```

---

## Task 7: バックフィル実行と動作検証

**Step 1: フロントエンドからバックフィルを実行**

ブラウザのdevtools consoleか、フロントエンドのユーティリティから:
```javascript
const { data, error } = await supabase.functions.invoke("backfill_embeddings");
console.log(data);
```

Expected: 各ソースタイプのprocessed/errors カウントが返る

**Step 2: embedding数を確認**

Supabase MCP execute_sql:
```sql
SELECT source_type, count(*) FROM document_embeddings GROUP BY source_type ORDER BY count DESC;
```

Expected: 各ソースタイプの件数が表示される

**Step 3: RAG検索をテスト**

Hub-AIチャットで質問を投げて、関連性の高い回答が返ることを確認:
- 「最近のメモの内容を教えて」
- 「今週届いたメールについて」
- 「プロジェクト○○の状況は？」

**Step 4: 全変更をまとめてコミット（未コミットがある場合）**

```bash
git add -A
git commit -m "feat: complete RAG with pgvector implementation"
```

---

## Task Summary

| Task | 内容 | 新規/変更 |
|------|------|----------|
| 1 | pgvector + document_embeddings テーブル | Migration (新規) |
| 2 | generate_embedding Edge Function | Edge Function (新規) |
| 3 | backfill_embeddings Edge Function | Edge Function (新規) |
| 4 | ai_hub_chat RAG統合 | Edge Function (変更) |
| 5 | useAiChat.ts フロントエンド改修 | Hook (変更) |
| 6 | DB Trigger設定 | Migration (新規) |
| 7 | バックフィル実行・動作検証 | 手動テスト |
